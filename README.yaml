---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: terraform-aws-cross-account-role

# Tags of this project
tags:
  - aws
  - terraform
  - terraform-modules
  - cross-account-role

# Categories of this project
categories:
  - terraform-modules/CICD

# Logo for this project
#logo: docs/slalom-logo.png

# License of this project
license: "APACHE2"

# Copyrights
copyrights:
  - name: "Slalom, LLC"
    url: "https://slalom.com"
    year: "2019"

# Canonical GitHub repo
github_repo: jameswoolfenden/terraform-aws-cross-account-role

# Badges to display
badges:
  - name: "Build Status"
    image: "https://travis-ci.com/JamesWoolfenden/terraform-aws-cross-account-role.svg?branch=master"
    url: "https://travis-ci.com/JamesWoolfenden/terraform-aws-cross-account-role"
  - name: "Latest Release"
    image: "https://img.shields.io/github/release/JamesWoolfenden/terraform-aws-cross-account-role.svg"
    url: "https://github.com/JamesWoolfenden/terraform-aws-cross-account-role/releases/latest"

related:
  - name: "terraform-aws-codebuild"
    description: "Making a Build pipeline"
    url: "https://github.com/jameswoolfenden/terraform-aws-codebuild"

# Short description of this project
description: |-
  Terraform module to provision a role in AWS.

# How to use this project
usage: |-
  Include this repository as a module in your existing terraform code:

  ```hcl
  module "role" {
    source     = "git::https://github.com/jameswoolfenden/terraform-aws-cross-account-role.git?ref=master"
    policy_arns       = "${var.policy_arns}"
    role_name         = "${var.role_name}"
    principal_arns    = "${var.principal_arns}"
  }
  ```

include:
  - "docs/terraform.md"

# Contributors to this project
contributors:
  - name: "James Woolfenden"
    github: "jameswoolfenden"
